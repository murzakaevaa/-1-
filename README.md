# лаб1 код-ревью

1 задание:
1. Форматирование и стиль кода
   
Отступы: исправлены с 4 пробелов на 2 пробела в соответствии с Google Style Guide

Положение скобок: открывающие скобки { перенесены на ту же строку, закрывающие } на отдельной строке

Имена переменных: изменены с сокращенных на понятные snake_case:

  n → item_count (количество предметов)

  maxWeight → max_total_weight (максимальный вес)

  dp → min_tax (минимальный налог для веса)

  selected → is_selected (матрица выбранных предметов)

  minVal → best_min_tax (лучший минимальный налог)

  bestWeight → best_weight (лучший вес)

  bestSelection → best_selection (лучший набор)

Имена функций: knapSack → find_optimal_artifacts (lower_case_with_underscores)

2. Удаление глобального пространства имен
   
Убрано using namespace std;

Добавлен префикс std:: ко всем стандартным типам и функциям

3. Локаль
   
Исправлена на std::setlocale(LC_ALL, "Russian");

4. Исправление логической ошибки
   
Было: поиск весов >= Z

Стало: поиск весов строго больше Z (min_weight + 1)

5. Обработка ошибок
   
Добавлена проверка открытия файла

Добавлен возврат кода ошибки return 1 при неудаче

Добавлен явный return 0 в конце main

6. Производительность вывода
   
Заменены std::endl на \n для избежания лишнего сброса буфера

2 задание:

1. Форматирование и стиль кода
    • Отступы: исправлены с 4 пробелов на 2 пробела
    • Положение скобок: открывающие скобки { перенесены на ту же строку
    • Длина строк: разбиты длинные строки (максимум 80 символов)

2. Исправление глобального пространства имен
    • Убрано using namespace std;
    • Добавлен префикс std:: ко всем стандартным типам и функциям

3. Исправление имен (Google Style)
    • Функции: findPath → find_max_path
    • Переменные:
        ◦ N → board_size
        ◦ docka → board
        ◦ dp → max_sum
        ◦ maxSum → max_sum_value
        ◦ x, y → row, col

4. Исправление логических ошибок
    • Было: движение начиналось с левого верхнего угла (0,0)
    • Стало: движение начинается с правого нижнего угла (row = board_size - 1, col = board_size - 1)
    • Было: условие цикла while (x < N - 1 || y < N - 1)
    • Стало: условие цикла while (row > 0 || col > 0)
    • Было: проверка границ if (x == N - 1) и if (y == N - 1)
    • Стало: проверка границ if (row == 0) и if (col == 0)
    • Было: сравнение dp[x + 1][y] > dp[x][y + 1] (движение вниз/вправо)
    • Стало: сравнение max_sum[row - 1][col] > max_sum[row][col - 1] (движение вверх/влево)
    • Было: инкременты x++, y++ (движение вниз/вправо)
    • Стало: декременты row--, col-- (движение вверх/влево)

5. Производительность вывода
    • Заменены std::endl на \n для избежания лишнего сброса буфера

6. Обработка ошибок
    • Добавлена проверка открытия входного файла
    • Добавлена проверка открытия выходного файла
    • Добавлен возврат кода ошибки return 1 при неудаче
    • Добавлен явный return 0 в конце main


3 задание:

1. Форматирование и стиль кода
    • Отступы: исправлены с 4 пробелов на 2 пробела
    • Положение скобок: открывающие скобки { перенесены на ту же строку
    • Длина строк: разбиты длинные строки (максимум 80 символов)

2. Исправление глобального пространства имен
    • Убрано using namespace std;
    • Добавлен префикс std:: ко всем стандартным типам и функциям

3. Исправление имен (Google Style)
    • Функции:
        ◦ Count → count_numbers_with_many_zeros
        ◦ HomeDyn16 → solve_task
    • Переменные:
        ◦ N → length
        ◦ K → base
        ◦ dp → dp (оставлено, т.к. это стандартное название для динамического программирования)
        ◦ all → total_numbers
        ◦ count → result_count

4. Исправление логических ошибок
    • Было: отсутствовала проверка условия N + K < 26
    • Стало: добавлена проверка с выводом сообщения об ошибке
    • Было: опечатка в слове "болше"
    • Стало: исправлено на "более"

5. Производительность вывода
    • Заменены std::endl на \n для избежания лишнего сброса буфера

6. Обработка ошибок
    • Добавлена проверка корректности введенных данных
    • Добавлен возврат из функции при неверных данных
    • Добавлен явный return 0 в конце main




4 задание:

1. Форматирование и стиль кода
    • Отступы: исправлены с 4 пробелов на 2 пробела
    • Открывающие скобки { перенесены на ту же строку

2. Исправление глобального пространства имен
    • Убрано using namespace std;
    • Добавлен префикс std:: ко всем стандартным типам и функциям (std::cout, std::cin, std::string, std::stoi)

3. Исправление имен 
    • Функции:
        ◦ calc → calculate_expression
        ◦ element → process_element
    • Переменные:
        ◦ str → expr_str / element_str (в зависимости от контекста)
        ◦ r → result

4. Удаление лишней функциональности
    • Функция term ПОЛНОСТЬЮ УДАЛЕНА (отвечала за умножение)
    • Убраны все проверки на символ *
    • Убраны все проверки на умножение

5. Удаление циклов (согласно заданию)
    • Убран цикл while для поиска оператора справа налево
    • Убран цикл while из удаленной функции term
    • Добавлен рекурсивный разбор строки слева направо
    • Добавлены рекурсивные функции find_matching_bracket и find_matching_bracket_from_pos для поиска парных скобок

6. Добавленные функции
    • is_digit - проверка, является ли символ цифрой
    • find_matching_bracket - рекурсивный поиск парной закрывающей скобки
    • find_matching_bracket_from_pos - вспомогательная рекурсивная функция

7. Обработка ошибок
    • Добавлена проверка на пустую строку
    • Добавлена проверка на пустые скобки
    • Добавлен явный return 0 в конце main

8. Производительность вывода
    • Заменены std::endl на \n для избежания лишнего сброса буфера


